[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q --strict-markers
markers =
    integration: requires a live Postgres (set TEST_DATABASE_URL)
filterwarnings =
    ignore::DeprecationWarning
